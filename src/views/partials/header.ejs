<!DOCTYPE html>
<html lang="en">
<head>
    <% if (NODE_ENV.includes('dev')) { %>
        <script>
            // Get the current hostname and port from the browser
            const host = window.location.hostname;
            const currentPort = window.location.port;

            // Calculate WebSocket port (current port + 1)
            const wsPort = currentPort ? (parseInt(currentPort) + 1) : 3001;

            // Create WebSocket connection using dynamic hostname and port
            const ws = new WebSocket(`ws://${host}:${wsPort}`);
            ws.onclose = () => {
                setTimeout(() => location.reload(), 2000);
            };
        </script>
    <% } %>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/main.css">
    <%- renderStyles() %>
    <%- renderScripts() %>
</head>
<body>
    <header>
        <div class="logoSection" title="Jems Creations Logo">
            <h1>Jems Creations</h1>
            <img src="/photos/Jems Logo Gem-resize.png" alt="Jems Creations Logo" class="logo">
        </div>

        
        <nav class="mainMenu">
            <input type="checkbox" id="menuToggle" hidden>
            <label class="menuIcon" for="menuToggle">☰</label>
            
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/products">Products</a></li>
                <li><a href="/login">login</a></li>
                <li><a href="/signup">Sign up</a></li>
                <li><a href="/dashboard">Dashboard</a></li>
                <li><a href="/cart">Cart</a></li>
                <li><a href="/contact">Contact</a></li>
                <li><a href="/createProduct">Create Product</a></li>
            </ul>
        </nav>
    </header>
<main>
